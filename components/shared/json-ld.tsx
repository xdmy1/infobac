// Thin re-export — the full set of JSON-LD builders lives in lib/seo/json-ld.tsx.
// Kept for backwards-compatibility with the homepage import.
export { HomepageJsonLd as JsonLd } from "@/lib/seo/json-ld";
